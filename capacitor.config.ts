import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.devparapalli.example',
  appName: 'example-capacitor-app',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
